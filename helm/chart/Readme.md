# Helm chart

Command to install helm charts

 helm install aks-chart-helm .

The idea is to use helm chart is to deploy the application with same config set to different environment and maintaining manifest templates.